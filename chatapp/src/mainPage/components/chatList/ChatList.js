import styled from "styled-components";

const ChatList = () => {
  return (
    <ChatListContainer>
      <p>For list of chats</p>
    </ChatListContainer>
  );
};

const ChatListContainer = styled.div`
  display: flex;
  width: 288px;
  background-color: #d3d8d7;
`;

export default ChatList;
